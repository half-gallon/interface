import { Box } from '@mui/material';

const AuthorizeScene = () => {
  return (
    <Box
      sx={{
        width: '100%',
        height: '100%',
      }}
    ></Box>
  );
};

export default AuthorizeScene;
