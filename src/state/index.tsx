import { atom } from 'jotai';

import { PAGE_STEPS } from './types';

// export const pageStepAtom = atom<PAGE_STEPS>(PAGE_STEPS.walletConnect);
export const pageStepAtom = atom<PAGE_STEPS>(PAGE_STEPS.registration);
